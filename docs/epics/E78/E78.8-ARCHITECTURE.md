# E78.8 — Architecture Diagram

## Patient Reactivation Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CLINICIAN TRIAGE INBOX UI                            │
│                    /clinician/triage (page.tsx)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌────────────┐  ┌────────────┐                                             │
│  │   Aktiv    │  │   Archiv   │  ← Toggle Buttons                           │
│  └────────────┘  └────────────┘                                             │
│                                                                               │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ Case List (filtered by showActive)                                    │  │
│  ├───────────────────────────────────────────────────────────────────────┤  │
│  │ Patient         │ Funnel   │ Status  │ Next Action    │ [ ⋯ ]        │  │
│  ├─────────────────┼──────────┼─────────┼────────────────┼──────────────┤  │
│  │ Max Mustermann  │ stress-1 │ Closed  │ None           │ [ ⋯ ] ◄──┐   │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                          │   │
│  ┌────────────────────────────────────────────────┐                     │   │
│  │ Action Dropdown (when [ ⋯ ] clicked)          │                     │   │
│  ├────────────────────────────────────────────────┤                     │   │
│  │  ⊙ Bestätigen                                  │                     │   │
│  │  ⚑ Markieren                                   │                     │   │
│  │  ✕ Markierung entfernen                        │                     │   │
│  │  🕐 Zurückstellen                              │                     │   │
│  │  ✓ Schließen                                   │                     │   │
│  │  ↻ Wiedereröffnen  ◄───────────────────────────┼─────────────────────┘   │
│  │  ⎘ Notiz hinzufügen                            │  Click to reactivate    │
│  └────────────────────────────────────────────────┘                         │
│                          │                                                   │
└──────────────────────────┼───────────────────────────────────────────────────┘
                           │
                           ▼
                  triageCaseReopen(caseId, { reason })
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            API LAYER                                         │
│          POST /api/clinician/triage/cases/{caseId}/reopen                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  1. Authenticate user (requires session)                                     │
│  2. Authorize role (clinician or admin)                                      │
│  3. Verify case exists and accessible                                        │
│  4. Check idempotency (is case already open?)                                │
│  5. Record action in triage_case_actions table                               │
│  6. Return success response                                                  │
│                                                                               │
└───────────────────────────────┬─────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         DATABASE LAYER                                       │
│                  PostgreSQL with Supabase                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────┐            │
│  │ triage_case_actions (Append-Only Audit Log)                 │            │
│  ├─────────────────────────────────────────────────────────────┤            │
│  │ id               │ UUID                                     │            │
│  │ assessment_id    │ Foreign key → assessments                │            │
│  │ patient_id       │ Foreign key → users                      │            │
│  │ action_type      │ 'reopen' ◄──── NEW ACTION RECORDED       │            │
│  │ payload          │ { "reason": "Patient wünscht..." }       │            │
│  │ created_by       │ Clinician user ID                        │            │
│  │ created_at       │ Timestamp                                │            │
│  └─────────────────────────────────────────────────────────────┘            │
│                                │                                              │
│                                ▼                                              │
│  ┌─────────────────────────────────────────────────────────────┐            │
│  │ triage_cases_v1 (Materialized View)                         │            │
│  ├─────────────────────────────────────────────────────────────┤            │
│  │ Computes effective state from HITL actions                  │            │
│  │                                                             │            │
│  │ WITH latest_close_reopen AS (                               │            │
│  │   SELECT DISTINCT ON (assessment_id)                        │            │
│  │     assessment_id,                                          │            │
│  │     action_type  ◄──── Get most recent close/reopen        │            │
│  │   FROM triage_case_actions                                  │            │
│  │   WHERE action_type IN ('close', 'reopen')                  │            │
│  │   ORDER BY assessment_id, created_at DESC                   │            │
│  │ )                                                            │            │
│  │                                                             │            │
│  │ is_active = CASE                                            │            │
│  │   WHEN latest_action = 'close' THEN false                   │            │
│  │   ELSE true  ◄──── Reopen makes case active!               │            │
│  │ END                                                          │            │
│  │                                                             │            │
│  │ next_action = CASE                                          │            │
│  │   WHEN status='in_progress' AND workup_status='needs_more   │            │
│  │     data' THEN 'patient_provide_data'                       │            │
│  │   WHEN status='in_progress' THEN 'patient_continue'         │            │
│  │   ... (automatic recalculation)                             │            │
│  │ END                                                          │            │
│  └─────────────────────────────────────────────────────────────┘            │
│                                                                               │
└───────────────────────────────┬─────────────────────────────────────────────┘
                                │
                                ▼
                    View automatically refreshes
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    TRIAGE INBOX API RESPONSE                                 │
│              GET /api/clinician/triage?activeOnly=true                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  Returns cases where is_active = true                                        │
│                                                                               │
│  {                                                                            │
│    "case_id": "...",                                                          │
│    "patient_id": "...",                                                       │
│    "case_state": "in_progress",                                              │
│    "is_active": true,  ◄──── Case is now active!                            │
│    "next_action": "patient_continue",  ◄──── Meaningful next action         │
│    ...                                                                        │
│  }                                                                            │
│                                                                               │
└───────────────────────────────┬─────────────────────────────────────────────┘
                                │
                                ▼
                    UI auto-reloads case list
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         UPDATED UI STATE                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  Active View (showActive = true):                                            │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ Patient         │ Funnel   │ Status      │ Next Action       │ [ ⋯ ]  │  │
│  ├─────────────────┼──────────┼─────────────┼───────────────────┼────────┤  │
│  │ Max Mustermann  │ stress-1 │ In Progress │ patient_continue  │ [ ⋯ ]  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                          ▲                                                   │
│                          │                                                   │
│                    Case moved here!                                          │
│                                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Key Components

### 1. UI Layer (E78.7)
- **File:** `/apps/rhythm-studio-ui/app/clinician/triage/page.tsx`
- **Responsibility:** Display cases, toggle views, trigger reopen action
- **Technology:** React 19, Next.js 16, TailwindCSS

### 2. API Layer (E78.4)
- **File:** `/apps/rhythm-studio-ui/app/api/clinician/triage/cases/[caseId]/reopen/route.ts`
- **Responsibility:** Authenticate, authorize, record action
- **Technology:** Next.js API Routes, Supabase Auth

### 3. Database Layer (E78.4 + E78.5)
- **Table:** `triage_case_actions` (append-only audit log)
- **View:** `triage_cases_v1` (effective state computation)
- **Technology:** PostgreSQL, Supabase RLS

## Security Layers

```
┌─────────────────────────────────────────┐
│ 1. Authentication (Session Required)    │
├─────────────────────────────────────────┤
│ 2. Authorization (Clinician/Admin Role) │
├─────────────────────────────────────────┤
│ 3. RLS (Org-Scoped Access)              │
├─────────────────────────────────────────┤
│ 4. Idempotency (Safe Duplicate Clicks)  │
└─────────────────────────────────────────┘
```

## Data Flow Summary

1. **User clicks "Wiedereröffnen"** in Archive view
2. **Optional reason prompt** appears
3. **API call** to `/reopen` endpoint
4. **Authentication** verified (session exists)
5. **Authorization** checked (clinician/admin role)
6. **Case access** verified (RLS check)
7. **Idempotency** checked (already open?)
8. **Action recorded** in `triage_case_actions`
9. **View refresh** triggers `triage_cases_v1` recalculation
10. **is_active** becomes `true`
11. **next_action** recalculated from assessment state
12. **UI reloads** case list
13. **Case appears** in Active view with meaningful next action

## Verification Points

✅ **Database:** `npm run verify:e78-4` (31/31 checks)  
✅ **UI:** `npm run verify:e78-7` (20/22 checks, reopen verified)  
✅ **Security:** RLS policies enforced  
✅ **Audit:** All actions logged with timestamp and user  
✅ **Idempotency:** Safe to call multiple times  
✅ **Next Action:** Automatically meaningful based on state
