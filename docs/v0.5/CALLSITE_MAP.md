# Callsite Map (v0.5)

Generated: 2026-01-10T14:08:10

Evidence commands:
```powershell
rg -n "/api/" app lib
```

## Endpoint -> Callers

### /api/account/deletion-request
- no known caller (string scan)

### /api/admin/content-pages
- no known caller (string scan)

### /api/admin/content-pages/[id]
- no known caller (string scan)

### /api/admin/content-pages/[id]/sections
- no known caller (string scan)

### /api/admin/content-pages/[id]/sections/[sectionId]
- no known caller (string scan)

### /api/admin/design-tokens
- no known caller (string scan)

### /api/admin/diagnostics/pillars-sot
- no known caller (string scan)

### /api/admin/funnels
- no known caller (string scan)

### /api/admin/funnels/[id]
- no known caller (string scan)

### /api/admin/funnel-step-questions/[id]
- no known caller (string scan)

### /api/admin/funnel-steps
- no known caller (string scan)

### /api/admin/funnel-steps/[id]
- no known caller (string scan)

### /api/admin/funnel-versions/[id]
- no known caller (string scan)

### /api/admin/funnel-versions/[id]/manifest
- no known caller (string scan)

### /api/admin/kpi-thresholds
- no known caller (string scan)

### /api/admin/kpi-thresholds/[id]
- no known caller (string scan)

### /api/admin/navigation
- no known caller (string scan)

### /api/admin/navigation/[role]
- no known caller (string scan)

### /api/admin/notification-templates
- no known caller (string scan)

### /api/admin/notification-templates/[id]
- no known caller (string scan)

### /api/admin/operational-settings-audit
- no known caller (string scan)

### /api/admin/reassessment-rules
- no known caller (string scan)

### /api/admin/reassessment-rules/[id]
- no known caller (string scan)

### /api/admin/usage
- no known caller (string scan)

### /api/amy/stress-report
- no known caller (string scan)

### /api/amy/stress-summary
- no known caller (string scan)

### /api/assessment-answers/save
- no known caller (string scan)

### /api/assessments/[id]/current-step
- no known caller (string scan)

### /api/assessments/[id]/navigation
- no known caller (string scan)

### /api/assessments/[id]/resume
- no known caller (string scan)

### /api/assessment-validation/validate-step
- no known caller (string scan)

### /api/auth/callback
- no known caller (string scan)

### /api/auth/resolve-role
- no known caller (string scan)

### /api/consent/record
- no known caller (string scan)

### /api/consent/status
- no known caller (string scan)

### /api/content/resolve
- no known caller (string scan)

### /api/content-pages/[slug]
- no known caller (string scan)

### /api/content-resolver
- no known caller (string scan)

### /api/documents/[id]/extract
- no known caller (string scan)

### /api/documents/[id]/status
- no known caller (string scan)

### /api/documents/upload
- no known caller (string scan)

### /api/funnels/[slug]/assessments
- no known caller (string scan)

### /api/funnels/[slug]/assessments/[assessmentId]
- no known caller (string scan)

### /api/funnels/[slug]/assessments/[assessmentId]/answers/save
- no known caller (string scan)

### /api/funnels/[slug]/assessments/[assessmentId]/complete
- no known caller (string scan)

### /api/funnels/[slug]/assessments/[assessmentId]/result
- no known caller (string scan)

### /api/funnels/[slug]/assessments/[assessmentId]/steps/[stepId]
- no known caller (string scan)

### /api/funnels/[slug]/content-pages
- no known caller (string scan)

### /api/funnels/[slug]/definition
- no known caller (string scan)

### /api/funnels/active
- no known caller (string scan)

### /api/funnels/catalog
- no known caller (string scan)

### /api/funnels/catalog/[slug]
- no known caller (string scan)

### /api/health/env
- no known caller (string scan)

### /api/notifications
- no known caller (string scan)

### /api/notifications/[id]
- no known caller (string scan)

### /api/patient/onboarding-status
- no known caller (string scan)

### /api/patient-measures/export
- no known caller (string scan)

### /api/patient-measures/history
- no known caller (string scan)

### /api/patient-profiles
- no known caller (string scan)

### /api/pre-screening-calls
- no known caller (string scan)

### /api/processing/content
- no known caller (string scan)

### /api/processing/delivery
- no known caller (string scan)

### /api/processing/jobs/[jobId]
- no known caller (string scan)

### /api/processing/pdf
- no known caller (string scan)

### /api/processing/ranking
- no known caller (string scan)

### /api/processing/risk
- no known caller (string scan)

### /api/processing/safety
- no known caller (string scan)

### /api/processing/start
- no known caller (string scan)

### /api/processing/validation
- no known caller (string scan)

### /api/reports/[reportId]/pdf
- no known caller (string scan)

### /api/review/[id]
- no known caller (string scan)

### /api/review/[id]/decide
- no known caller (string scan)

### /api/review/[id]/details
- no known caller (string scan)

### /api/review/queue
- no known caller (string scan)

### /api/shipments
- no known caller (string scan)

### /api/shipments/[id]
- no known caller (string scan)

### /api/shipments/[id]/events
- no known caller (string scan)

### /api/support-cases
- no known caller (string scan)

### /api/support-cases/[id]
- no known caller (string scan)

### /api/support-cases/[id]/escalate
- no known caller (string scan)

### /api/tasks
- no known caller (string scan)

### /api/tasks/[id]
- no known caller (string scan)

