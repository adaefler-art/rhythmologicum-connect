# Ensure stable line endings across OSes for drift-sensitive files
# NOTE: This does not rewrite existing files until you run: git add --renormalize .

# Canonical DB schema dump must be LF-only
schema/schema.sql text eol=lf

# Shell scripts must be LF-only to avoid bash CRLF issues
*.sh text eol=lf

# GitHub Actions workflows are LF-only
.github/workflows/*.yml text eol=lf
