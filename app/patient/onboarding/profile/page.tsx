import { ProfileClient } from './client'

export const metadata = {
  title: 'Profil erstellen | Rhythmologicum Connect',
  description: 'Erstellen Sie Ihr Basisprofil',
}

export default function ProfilePage() {
  return <ProfileClient />
}
